# .bequite/templates/
Reusable project templates from `/bq-release template` (alpha.24). Files created on first run — see `docs/specs/RELEASE_TEMPLATE.md`. Demo data only; explicit tenancy isolation.
