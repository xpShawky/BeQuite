# Live Edit Log

Append-only chronicle of every `/bq-live-edit` call. Newest at top.

---

<!--
  Template per edit:

  ## <ISO 8601 UTC> — <one-line request>

  **Section:** <name from SECTION_MAP>
  **File(s) changed:** <list>
  **Before:**
  ```
  <diff snippet, ~3 lines>
  ```
  **After:**
  ```
  <diff snippet, ~3 lines>
  ```
  **Test/check run:** <commands + results>
  **Screenshots:**
    - .bequite/uiux/screenshots/before-<ts>-<section>.png
    - .bequite/uiux/screenshots/after-<ts>-<section>.png
  **Notes:** <free-form>
-->

(no edits logged yet — run `/bq-live-edit` to add entries)
