---
description: Generate a complete handoff package for a second engineer. Writes HANDOFF.md at repo root with engineer-friendly + vibe-handoff-friendly sections. Plus a checklist the receiver should verify.
---

# /bq-handoff — generate a project handoff

You are creating a **handoff package** so a second engineer (or non-engineer recipient) can pick up this project cold and be productive.

## Step 1 — Read everything BeQuite knows

- `.bequite/audits/DISCOVERY_REPORT.md`
- `.bequite/audits/DOCTOR_REPORT.md`
- `.bequite/audits/FULL_PROJECT_AUDIT.md` (if recent)
- `.bequite/audits/VERIFY_REPORT.md`
- `.bequite/plans/IMPLEMENTATION_PLAN.md`
- `.bequite/plans/SCOPE.md`
- `.bequite/tasks/TASK_LIST.md`
- `.bequite/state/DECISIONS.md`
- `.bequite/state/OPEN_QUESTIONS.md`
- `.bequite/logs/CHANGELOG.md`
- README.md, AGENTS.md, CLAUDE.md

## Step 2 — Build the handoff at repo root

Write `HANDOFF.md`:

```markdown
# HANDOFF.md

**Project:** <name>
**Handoff date:** <ISO 8601>
**From:** <git config user.name or "previous owner">
**Generated by:** /bq-handoff

---

## For engineers

### TL;DR

(3-5 sentences: what this project is, what state it's in, the most important thing the receiver should know.)

### Quickstart

```bash
git clone <remote>
cd <name>
<install command>
<dev command>
```

Expected:
- <e.g. http://localhost:3000 renders the home page>

If that doesn't work, see [troubleshooting](#troubleshooting).

### Stack

| Layer | Tech |
|---|---|
| Runtime | <from DISCOVERY> |
| Framework | <> |
| Database | <> |
| Auth | <> |
| Hosting | <> |
| CI | <> |

### Architecture

(2-4 paragraphs + ASCII diagram from IMPLEMENTATION_PLAN.md)

### Repository layout

```text
<tree of top-level directories with a one-line description each>
```

### Key decisions made

(from DECISIONS.md — chronological)

| Date | Decision | Rationale |
|---|---|---|
| ... | ... | ... |

### What's done

(from TASK_LIST.md — items with `[x] done`)

### What's not done (open work)

(from TASK_LIST.md — `[ ] pending` + `[!] blocked`)

### Known issues

(from ERROR_LOG.md — recent unresolved items + RED_TEAM findings still open)

### How to run common operations

- Install:  `<command>`
- Dev:      `<command>`
- Test:     `<command>`
- Build:    `<command>`
- Release:  see [release runbook](#release-runbook)

### Release runbook

(from /bq-release command output, generalized)

### Troubleshooting

| Symptom | Likely cause | Fix |
|---|---|---|
| <e.g. dev server crashes on boot> | <e.g. missing .env> | <e.g. cp .env.example .env + fill in vars> |
| ... | ... | ... |

### Where to ask for help

- README.md
- `.bequite/audits/` (full audit history)
- `.bequite/state/DECISIONS.md` (why things are the way they are)
- Original maintainer: <user>

---

## For non-engineers (vibe-handoff section)

### What this product does

(plain-English description, 2-3 paragraphs, no jargon)

### Who uses it

(user types + their use cases)

### What's working right now

(features that are user-visible and stable)

### What's coming next

(features planned per SCOPE.md OUT section, with target versions)

### How much does it cost to run

(rough estimate of monthly hosting/service costs at current scale)

### Who do I call when something breaks

(escalation path)

---

## Receiver's checklist

When you receive this handoff, verify:

- [ ] You can clone the repo
- [ ] You can run the install command
- [ ] You can boot the dev server
- [ ] You can run the tests + they pass
- [ ] You can build for production
- [ ] You have access to the deployed environment
- [ ] You can deploy a small change end-to-end
- [ ] You understand the IN/OUT/NON-GOALS from SCOPE.md
- [ ] You've read the recent CHANGELOG entries
- [ ] You know where to find: tests, plans, decisions, audits

If any of these fails, contact the previous owner before continuing.

---

## Generated by BeQuite

This handoff is generated from `.bequite/` memory. See:

- `.bequite/audits/` — full project audits
- `.bequite/plans/` — implementation plans
- `.bequite/state/DECISIONS.md` — decision history
- `.bequite/logs/CHANGELOG.md` — release history
- `.bequite/logs/ERROR_LOG.md` — bug history + fixes
```

## Step 3 — Update state + log

- `.bequite/state/LAST_RUN.md` → `/bq-handoff`
- `.bequite/logs/AGENT_LOG.md` appended

## Step 4 — Report back

```
✓ Handoff generated

Engineer section:     <line count>
Vibe section:         <line count>
Receiver checklist:   <count> items

Output: HANDOFF.md at repo root

Suggest the receiver:
  1. Read HANDOFF.md
  2. Clone the repo
  3. Walk through the receiver's checklist
  4. Ask questions BEFORE making changes
```

## Rules

- **Two audiences.** Engineer section is technical; vibe-handoff section is plain-English. Both must be present.
- **Receiver checklist is concrete.** Each item is a yes-or-no.
- **No "tribal knowledge."** If a fact isn't in `.bequite/` memory, ask the user to add it before generating the handoff.
- **Cite where each fact came from.** "Stack: Next.js 15 (DISCOVERY_REPORT.md §3.1)" is better than "Stack: Next.js 15."

## Memory files this command reads

- All `.bequite/*` (broad)
- README.md, AGENTS.md, CLAUDE.md

## Memory files this command writes

- `HANDOFF.md` at repo root (new)
- `.bequite/state/LAST_RUN.md`
- `.bequite/logs/AGENT_LOG.md`

## Usual next command

(none — handoff is typically the final action. After the receiver picks up, they run `/bq-recover` or `/bequite` to onboard.)
