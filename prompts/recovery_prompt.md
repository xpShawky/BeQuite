# prompts/recovery_prompt.md

> **Resume from here in a new session without chat history** (master §25). Generated by `/recover` and `bequite recover`. Paste-able into Claude Code, Cursor, Codex, Aider, or any host that loads AGENTS.md.

---

You are resuming **BeQuite by xpShawky**.

**Do not rely on chat history.** Read the listed files; resume from the next safe task; do not restart the whole project.

---

## Files to read first (in order)

1. `AGENTS.md` — universal entry rules.
2. `CLAUDE.md` — Claude-Code-specific extensions (if you are Claude Code).
3. `.bequite/memory/constitution.md` — Iron Laws + active Doctrines (binding).
4. `.bequite/memory/projectbrief.md` — what this project is.
5. `.bequite/memory/productContext.md` — why it exists + user journeys.
6. `.bequite/memory/systemPatterns.md` — architecture + ADR index.
7. `.bequite/memory/techContext.md` — pinned stack + dev setup.
8. `.bequite/memory/activeContext.md` — what's happening NOW.
9. `.bequite/memory/progress.md` — evolution log.
10. `state/project.yaml` — operational state.
11. `state/current_phase.md` — current sub-version.
12. `state/recovery.md` — what's complete, what's incomplete, next safe task.
13. `state/task_index.json` — atomic task list.
14. `state/decision_index.json` — accepted ADRs.
15. `state/evidence_index.json` — evidence + receipts.
16. The most recent receipt (if v0.7.0+) at `.bequite/receipts/`.
17. The last 5 entries in `git log --oneline`.

---

## Then answer (out loud, in your reply to the owner)

1. **What is complete?** — list every sub-version + tag from `git tag -l`. Cross-reference `state/evidence_index.json` for each.
2. **What is incomplete?** — pull the `pending` and `in_progress` items from `state/task_index.json`.
3. **What failed last?** — read `state/recovery.md::What failed last`.
4. **What evidence exists?** — list `evidence/<phase>/<task>/` directories. Note which carry receipts (v0.7.0+).
5. **What is the next safe task?** — pull from `state/recovery.md::What is the next safe task`.
6. **What commands should run first?** — pull from `state/recovery.md::Commands to run first`.
7. **What files must not be touched?** — pull from `state/recovery.md::Files to NOT touch`.

---

## Then continue

Resume **only from the next safe task**. Update `state/recovery.md` + `.bequite/memory/activeContext.md` + `.bequite/memory/progress.md` before stopping.

**Do not restart the whole project.** Fix the smallest failing unit (master §3.7).

---

## Iron Laws (binding always — if these conflict with anything else, these win)

1. Specification supremacy — code serves the spec.
2. Verification before completion — banned weasel words: `should`, `probably`, `seems to`, `appears to`, `I think it works`, `might work`, `hopefully`, `in theory`.
3. Memory discipline — read all six Memory Bank files + active ADRs at session start.
4. Security & destruction discipline — never read `.env*`; never run destructive ops without ADR; never bypass hooks.
5. Scale honesty — declared scale tier is binding.
6. Honest reporting — what was built, what was tested, what remains, what is uncertain. All four.
7. Hallucination defense — never import a package without verifying it exists in this session.

---

## When in doubt

- Iron Law beats Doctrine.
- Doctrine beats convenience.
- ADR (`status: accepted`) beats convention.
- Active session evidence beats memory of a previous run.
- Ask the owner only when the question changes architecture, scale, security, compliance, cost, UX, automation depth, licensing, integration, testing, backup, or release.

---

## Project mode (binding for this session)

`state/project.yaml::mode` — Fast / Safe / Enterprise. Read it; respect its gating.

---

## End of recovery prompt
